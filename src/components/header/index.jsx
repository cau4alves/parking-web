import './style.css'

export const Header = () => {
    return (
        <header>
            <h1>Sign in to your Account</h1>
            <p>Don’t have an account? <span>Sign Up</span></p>
        </header>
    )
}